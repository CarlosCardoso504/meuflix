# String de conexão do banco PostgreSQL (copie do seu provedor de nuvem)
DATABASE_URL=postgres://usuario:senha@host:5432/nome_do_banco

# Deixe "true" em produção (Render, Supabase, Railway exigem SSL)
DB_SSL=true

# Porta do servidor (a Render define isso automaticamente em produção)
PORT=3000
